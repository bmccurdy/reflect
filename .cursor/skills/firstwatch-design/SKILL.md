---
name: firstwatch-design
description: Generate FirstWatch-branded UI for this Reflect prototype using the vendored design system. Use when creating or editing screens, components, styles, or HTML in this repo.
---

Read `vendor/firstwatch/SKILL.md` and `vendor/firstwatch/readme.md` before designing or coding UI.

This repo is a static HTML prototype. Link `vendor/firstwatch/styles.css` and use `fw-*` classes. Do not import React component JSX.

Staff surfaces stay on kit tokens and classes. The patient survey is the only allowed visual extension (`--p-*` in `index.html`).
